{
  "name": "flipper-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "ns-agent": "npm run start --workspace=@flipper-agents/ns-agent",
    "f5-agent": "npm run start --workspace=@flipper-agents/f5-agent"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
